## 常用命令

本仓库各产物多为**纯静态 HTML**（`deck/`、`stepfun-deck/`、`stepfun-v2/` 等），没有统一的构建/测试工具链——直接用浏览器打开对应目录的 `index.html` 即可（← → / 空格 / 点击翻页）。截图预览走 `visual-deck-main` skill 的 `preview.sh`（Windows 上的坑见全局 memory `preview-sh-windows-fix.md`）。

测试：本仓库目前**没有测试套件**。`verify-step-*.png` 是用 puppeteer-core 截图做的人工校验产物，不是自动化测试。
